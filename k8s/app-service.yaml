apiVersion: v1
kind: Service
metadata:
  name: projecthub-service
  namespace: projecthub-backend
  labels:
    app: projecthub
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: projecthub
